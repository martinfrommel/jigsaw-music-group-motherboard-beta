import type { Meta, StoryObj } from '@storybook/react'

import SubmitReleasePage from './SubmitReleasePage'

const meta: Meta<typeof SubmitReleasePage> = {
  component: SubmitReleasePage,
}

export default meta

type Story = StoryObj<typeof SubmitReleasePage>

export const Primary: Story = {}
